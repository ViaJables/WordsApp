import 'package:flutter/material.dart';

class BannerAdBox extends StatelessWidget {

  @override
  Widget build(BuildContext context) {
    return Container(
//      width: double.infinity,
//        child: AdmobBanner(adUnitId: bannerId, adSize: AdmobBannerSize.BANNER)
    );
  }
}
